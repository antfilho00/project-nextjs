import styled from 'styled-components';

export const LogoLink = styled.a`
  display: flex;
  text-decoration: none;
  color: inherit;
  display: flex;
  align-items: center;

  > img {
    height: 3rem;
  }
`;
